# Resume Builder Agent - Python Dependencies
# Install: pip3 install -r requirements.txt

python-docx==1.1.2

# PDF generation (Windows only — requires MS Word installed)
# Install separately on Windows: pip install docx2pdf
docx2pdf==0.10; sys_platform == "win32"

# Testing
pytest==9.0.3
pytest-tmp-files==0.0.2
