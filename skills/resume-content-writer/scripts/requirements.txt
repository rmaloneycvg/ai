# Resume Builder Agent - Python Dependencies
# Install: pip3 install -r requirements.txt
# Note: docx2pdf is installed on Windows Python separately (pip install docx2pdf)

python-docx==1.1.2

# Testing
pytest==8.3.3
pytest-tmp-files==0.0.2
