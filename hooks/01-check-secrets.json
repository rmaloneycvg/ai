{
  "version": "v1",
  "hooks": [
    {
      "name": "check-secrets",
      "description": "Block writes containing credential-shaped strings (AWS keys, private keys, GitHub/Slack tokens, generic API keys). Reads the content, text, and newStr shapes because different write tools send different field names.",
      "trigger": "PreToolUse",
      "matcher": "fs_write|write|str_replace|fs_append",
      "action": {
        "type": "command",
        "command": ".kiro/hooks/scripts/check-secrets.sh"
      },
      "timeout": 10
    }
  ]
}