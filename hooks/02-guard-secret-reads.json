{
  "version": "v1",
  "hooks": [
    { "name": "guard-secret-reads",
      "description": "Block read_file/read_files calls targeting known secret files (SSH keys, cloud credentials, etc.). Pattern-based scan tools are intentionally NOT matched, so grep_search/file_search/list_directory stay unaffected.",
      "trigger": "PreToolUse",
      "matcher": "read_file|read_files",
      "action": { "type": "command", "command": ".kiro/hooks/scripts/guard-secret-reads.sh" },
      "timeout": 10, "enabled": true } ]
}